<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<DialogPane
    fx:id="dialogPane"
    HBox.hgrow="NEVER"
    VBox.vgrow="ALWAYS"
    xmlns="http://javafx.com/javafx/8"
    xmlns:fx="http://javafx.com/fxml"
>
    <content>
        <VBox maxWidth="Infinity" spacing="8" HBox.hgrow="ALWAYS" fx:id="verticalHolder">
            <VBox maxWidth="Infinity" spacing="15" HBox.hgrow="ALWAYS">
                <VBox spacing="5" HBox.hgrow="ALWAYS" maxWidth="Infinity" alignment="CENTER_LEFT">
                    <Label text="Categories (you may only delete categories without associations):"/>
                    <ListView
                        prefWidth="200"
                        minWidth="200"
                        maxHeight="200"
                        prefHeight="200"
                        fx:id="categoriesView"
                        HBox.hgrow="ALWAYS"
                        maxWidth="Infinity"
                    />
                    <GridPane hgap="10" HBox.hgrow="ALWAYS" maxWidth="Infinity" fx:id="categoryButtonsPane">
                        <Button
                            GridPane.rowIndex="0"
                            GridPane.columnIndex="0"
                            text="Remove category"
                            fx:id="removeCategoryButton"
                            HBox.hgrow="ALWAYS"
                            maxWidth="Infinity"
                            styleClass="button"
                        />
                        <Button
                            GridPane.rowIndex="0"
                            GridPane.columnIndex="1"
                            text="Add category"
                            fx:id="newCategoryButton"
                            HBox.hgrow="ALWAYS"
                            maxWidth="Infinity"
                            styleClass="button"
                        />
                    </GridPane>
                </VBox>
                <HBox spacing="5" HBox.hgrow="ALWAYS" maxWidth="Infinity" alignment="CENTER_RIGHT">
                    <Label text="Default password length:"/>
                    <Spinner maxWidth="80" amountToStepBy="1" prefWidth="80" fx:id="passwordLengthSpinner" />
                </HBox>
                <VBox spacing="3" HBox.hgrow="ALWAYS" maxWidth="Infinity">
                    <HBox spacing="5" HBox.hgrow="ALWAYS" maxWidth="Infinity" alignment="CENTER_RIGHT">
                        <Label text="Minutes to reuse entered password for subsequent operations:"/>
                        <Spinner maxWidth="80" amountToStepBy="1" prefWidth="80" fx:id="dedupeTimeSpinner" />
                    </HBox>
                    <!-- the font family is needed: the System font has no usable italic face, so -fx-font-style on
                    its own silently renders upright. sans-serif is generic and resolves on both mac and Windows -->
                    <Label
                        fx:id="dedupeTimeNoteLabel"
                        wrapText="true"
                        maxWidth="Infinity"
                        alignment="CENTER_RIGHT"
                        textAlignment="RIGHT"
                        style="-fx-text-fill: red;"
                    />
                </VBox>
            </VBox>
        </VBox>

    </content>
    <buttonTypes>
        <ButtonType text="Save" fx:id="okButton" buttonData="OK_DONE"/>
    </buttonTypes>
</DialogPane>
